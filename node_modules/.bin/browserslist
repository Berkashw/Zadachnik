#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@vue/cli-plugin-babel/node_modules/core-js-compat/node_modules/browserslist/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../@vue/cli-plugin-babel/node_modules/core-js-compat/node_modules/browserslist/cli.js" "$@"
  ret=$?
fi
exit $ret
